While `cd` changes your working directory, `pwd` shows your working directory.


`rm` removes a file.


`mv` moves a file.


`cp` copies a file.


`ln` creates a link.


`head` and `tail` look at the first or last lines of a file, respectively.


While `more` lets you go through a file's text.

`echo` rewrites the file to have just that text
`cat` adds on text to the end of a file
EOFs can allow you to write larger sections like this, specifying with the EOF line when you're actually done
My samtools version is 1.24.

This is code that could be used to create a new file:

 echo Test words > ~vmabr/workhouse/test.txt
 
This creates a text file with these words, starting from the home directory
This is another example: ```tree > example_directory/folder1/tree.txt```
 
This is an example of a relative path from my current working directory.
 
This is what the absolute directory for that same file would look like: ```/home/vmabr/appbio-2026/week01/assignment1/example_directory/folder1/tree.txt```
 Finally, I chose Visual Studio as my AI ready editor.
